version: "3.8"

services:
  writer:
    image: alpine:3.19
    user: "1001:1001"
    volumes:
      - ./data:/data
      - ./writer.sh:/writer.sh:ro
    command: ["sh", "/writer.sh"]
    restart: always
